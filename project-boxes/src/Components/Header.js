import React from "react";

const Header = () => {
    return (
        <header>
            <div className="logo">
                <a href="/">Logo</a>
            </div>
        </header>
    );
};

export default Header;